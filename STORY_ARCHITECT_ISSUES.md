# Story Architect — Issue Tracker & Improvement Roadmap

**Total Issues: 24** (15 user-reported + 3 from code review + 6 additional discoveries)

---

## Phase 1: Quick Wins & Critical Fixes

| # | Priority | Issue | Severity | Category | Current Status | Recommended Fix | Potential Problems from Fix | Mitigations for Fix Problems | Effort | Notes / Additional Context |
|---|----------|-------|----------|----------|----------------|-----------------|---------------------------|------------------------------|--------|--------------------------|
| 1 | P1 | Feature flag was not active for prior sessions | Critical | Config | Resolved — flag now set | Verify by running a test session and checking for cultural brief data in the session JSON. Add ENABLE_CULTURAL_ENGINE=true to any .env.example or deployment docs. | None — already fixed. Risk only if deployed without the env var. | Add the var to `.env.example` with a comment. Add a startup check that logs a warning if the flag is missing. Long-term: remove the flag entirely and make the engine always-on. | Trivial | Consider removing the feature flag entirely once validated, since it should always be on. |
| 2 | P1 | Cultural engine never tested end-to-end | Critical | Testing | Unknown — needs verification | Run a test session with a culturally rich seed. After clarifier turn 2, inspect session JSON for cultural brief data. Check server logs for [CULTURAL] prefixed messages. | If engine produces bad briefs, they could harm output quality. Need the feature flag as a kill switch. | Keep the feature flag as the kill switch during the test phase. Run 3 test sessions with different cultural contexts and manually review the briefs before removing the flag. Add a brief quality score in the researcher output — if below threshold, discard the brief silently rather than injecting garbage. If Haiku briefs are thin, promote `cultural_researcher` to Sonnet in modelConfig — isolated change, no code refactor needed. | Low | The two-LLM-call architecture on Haiku may produce thin briefs. May need Sonnet for the researcher role. |
| 3 | P1 | sceneService has zero cultural engine integration | High | Code Gap | Confirmed — no cultural imports | Add the same integration pattern from hookService/characterService: imports, helpers, brief injection into clarifier/builder prompts, background research firing. | Increases scene token cost (2 extra Haiku calls per scene). Could slow an already-slow module. Scene prompts are very long — cultural context may push against context limits. | Only fire cultural research for scenes where `shouldRunCulturalResearch()` throttle passes AND the scene involves culturally relevant beats (check if beat references setting, customs, dialogue, or character background). For context limits, cap cultural brief injection at 300 tokens in scene prompts — the `formatBriefForBuilder()` method can be given a `maxTokens` parameter. Combined with Issue 12 (scene speed), batch building offsets the per-scene cost increase. | Medium | Scenes are final creative output — cultural grounding matters most here. Aggressively throttle cultural calls. Consider only firing for culturally relevant scenes. |
| 4 | P2 | Divergence + cultural engine outputs invisible to user | High | UX Gap | Confirmed — no UI surface | Add a "Debug/Insights" panel via button on each module's clarifier screen. Show: cultural brief with evidence, divergence direction map, psychology signals. Use collapsible drawer. | Users may over-rely on debug info and second-guess the engine. Creates "backseat driver" behavior. Adds frontend complexity. | Make the panel "developer mode" — hidden behind a settings toggle, off by default. Label it clearly: "Engine Internals — for testing, not steering." Show data as read-only with no edit controls. Use a slide-out drawer that doesn't disrupt the main flow. Phase rollout: start with just cultural + divergence data; add psychology signals later once the panel is validated. | Medium-High | Essential for testing. Consider a "developer mode" toggle. Could also log to a separate "engine insights" tab per session. |
| 23 | P1 | Character sex/gender gets mixed up in image generation — not a first-class field anywhere | Critical | Architecture / Data Model | Fragile — pronoun-based fallback only | **Four-layer fix:** (1) Add `gender` field to `CharacterProfile` and `CharacterPack.locked.characters`. (2) REQUIRE gender confirmed per character before readiness. (3) Builder prompt requires `gender` as mandatory output. (4) `buildCharacterIdentities()` reads discrete field directly. (5) `image_generation_prompt` derives "1girl/1boy" from locked field. | Making gender mandatory forces a choice that may not fit all stories (non-binary, genderfluid, ambiguous). Adds a question to every session. Some users may find explicit gender selection uncomfortable. | Add a "presentation" field instead of strict binary gender — options: "masculine", "feminine", "androgynous", "fluid/varies", "prefer not to specify." Map each to image gen tags: masculine → "1boy", feminine → "1girl", androgynous → "1person, androgynous", fluid → ask user per-scene. "Prefer not to specify" uses the description + pronoun fallback (existing code) but with a UI warning: "Visual generation works best with a presentation choice." Surface as an assumption on turn 1 — not a form field — framed as: "I'm picturing the protagonist as [X] — does that feel right?" with vivid alternatives. Extends to age range and ethnicity using the same pattern — discrete optional fields with smart defaults from context. | Medium | **Root cause:** Gender flows as freeform text in `description` strings and optional ledger entries with inconsistent keys. Same problem applies to age, race/ethnicity, body type. Issue 8 (character editing screen) also fixes this via direct override. |

---

## Phase 2: Story Quality & User Control

| # | Priority | Issue | Severity | Category | Current Status | Recommended Fix | Potential Problems from Fix | Mitigations for Fix Problems | Effort | Notes / Additional Context |
|---|----------|-------|----------|----------|----------------|-----------------|---------------------------|------------------------------|--------|--------------------------|
| 5 | P2 | Story length always the same (12-20 beats hardcoded) | High | Prompt/Arch | Confirmed — beat count doesn't scale with scope | Make plot builder beat count dynamic. Read scope/length from hook constraint ledger. Map: "gut-punch" → 6-10 beats, "standard" → 12-18, "slow-burn" → 20-35, "episodic" → 30-50+. Pass range into builder prompt. | Very long stories (30+ beats) may exceed context limits. Beat quality degrades at high counts. Scene module would need 20+ scenes, compounding slowness. May need chunked building. | For 30+ beats, split the builder into 2-3 "arc" calls: Act 1 (beats 1-10), Act 2 (beats 11-22), Act 3 (beats 23-35). Each arc call receives the prior arcs' output as context. This avoids context overflow and maintains beat quality. For scene count, pair with Issue 12's "Generate All" mode — bulk generation makes high scene counts viable. Add a user-facing warning when scope implies 20+ scenes: "This will be a long story — generation may take several minutes." Cap at 50 beats maximum to prevent runaway generation. | Medium | The scope/length signal already exists — it just doesn't flow into beat count. For very long stories, consider a "chapter" abstraction between plot and scene. |
| 6 | P2 | No "loose ends resolver" before scene generation | High | Architecture | Confirmed — dev targets exist but invisible | Add "Pre-Scene Audit" between plot lock and scene planning: aggregate unresolved development targets from all judges, present as checklist with severity/fix options, let user address or accept as-is. | Adds another interaction step. Users wanting fast generation will be annoyed. Could create "too many problems" feeling. Some issues may not be fixable at this stage. | Make it auto-skip when zero unresolved targets exist — user sees "All clear, proceeding to scenes." When targets exist, show as a single screen (not per-target), grouped by severity: "Critical" (blocks coherent scenes), "Worth reviewing" (quality improvements), "Minor" (skippable). Default all Minor targets to "accept as-is." Add a "Skip audit, I trust the system" button that accepts all targets. For unfixable-at-this-stage targets, label them: "This will be addressed automatically during scene writing" (which is true — the upstream target system already does this). Limit to max 5 visible targets — rest collapse into "and 3 more minor items." | High | The upstream dev target system is robust — data exists. Make it skippable: "Everything looks good — proceed?" vs "We found 3 things worth reviewing." |
| 7 | P2 | Characters default to ~4 regardless of story | Medium | Prompt/LLM | Partially addressed — prompt says 4-6 but LLM anchors at 4 | Three changes: (1) Surface cast size as explicit assumption with vivid alternatives. (2) Tie cast size to scope/length. (3) Builder prompt gets exact count: "Generate exactly [N] character profiles." | Larger casts increase cost, complexity, and scene count. Characters 5+ get less depth. More clarifier turns needed. Supporting characters risk becoming "furniture." | For depth at scale: when cast > 5, auto-extend clarifier by 1-2 turns (the pacing guidance already adjusts per turn count). Add "tiered depth" — protagonist + antagonist get full 20-dial treatment, supporting characters get a reduced 8-dial profile (want, misbelief, stakes, role_function, relationship, stress_style, tell, voice). This cuts builder token cost ~40% for supporting roles while keeping them distinct. For "furniture" risk, the judge's diversity score already catches this — just make it a hard-fail for cast > 5 (currently soft). Tie the cast size assumption to Issue 10's hook-level discussion so users decide early. | Medium | The prompt saying "4-6" anchors the LLM at 4. An explicit user-facing assumption gives direct control. Real challenge is maintaining quality at scale. |
| 8 | P2 | No opportunity to edit character details (race, age, name) before builder | High | UX Gap | Confirmed — no pre-builder review | Add "Character Review" screen after clarifier readiness, before builder fires. Show resolved state with editable fields for name, age, race, gender, physical description. System provides recommendations, user can override. | Adds mandatory step. Could break "fun" flow if it feels like a form. Users may make conflicting choices (age 8 in adult romance). Need validation/warnings. | Frame as a fun reveal, not a form: "Here's who I think your cast is — any last tweaks?" Show each character as a card with smart defaults pre-filled from context (cultural engine for names, LLM inference for age/appearance). Make the screen swipeable/interactive, not a spreadsheet. Editable fields are inline — tap to change, not separate form inputs. For conflicting choices, add soft validation: "Heads up — a character aged 8 may not fit the adult themes in your story. Keep anyway?" (not blocking). For users who want to skip: "Looks perfect, let's go" button that accepts all defaults. Time budget: add 15-30 seconds to the pipeline — acceptable given it prevents wrong-gender/wrong-race images that require full regeneration (minutes). | Medium-High | Key user request. Make it feel creative: "Meet your cast — any last changes?" Consider tying to cultural engine for culturally appropriate name/description suggestions. |
| 9 | P2 | No warning when user steers story badly / issues not surfaced with options | Medium | Prompt/UX | Partially addressed — conflict_flag exists but limited | Strengthen conflict_flag: (1) Add "story health score" aggregating flags. (2) Prominent warning with fix options when health drops. (3) Quality gate before builder where flags must be resolved or accepted. (4) Give 2-3 concrete fix options per flag. | Over-warning kills creative freedom. Quality gates could block valid creative visions. Edge cases trigger false positives. Need "I know what I'm doing" escape hatch. | Implement a three-tier warning system: **Soft** (yellow, informational, auto-dismissible) — "This combination is uncommon but could work." **Moderate** (orange, requires acknowledgment) — "These choices conflict — here are 3 ways to resolve." **Hard** (red, blocks builder, rare) — "This will produce an incoherent story." Only Hard blocks progress, and even then includes "I understand, proceed anyway." Track false positive rate: if a user dismisses > 3 warnings in a session, reduce sensitivity for that session (they're a director, not an explorer). Never show more than 1 warning per turn. Conflict_flag options should be concrete story pivots, not abstract advice: "Option A: Darken the tone to match the stakes / Option B: Lower the stakes to match the comedy / Option C: Keep both — lean into tonal whiplash as a feature." | Medium | conflict_flag is the right mechanism — needs prominence in UI and actionable options. The prompt already says "The UI will show this as a warning" — options need to be concrete choices. |
| 10 | P2 | Cast count never discussed with user in hook module | Medium | Prompt Gap | Not addressed | Add "CAST SIZE" creative constraint to hook clarifier as an assumption. Map: "intimate duo" (2-3), "pressure triangle" (3-4), "ensemble crew" (5-7), "faction epic" (7+). Carry confirmed size into character module. | Premature in hook module — users may not know yet. Could feel like a form question. | Don't surface on turn 1 — wait until turn 2-3 when the story shape is forming. Frame as a story-feel assumption, not a number: "I'm picturing this as a tight two-person pressure cooker" with alternatives like "Actually more of a crew dynamic with shifting alliances." Include a "not sure yet" option that defers to the character module (where the clarifier can revisit). If deferred, the character module auto-surfaces it on turn 1. The LLM should infer from the seed when possible — "a story about a royal court" implies ensemble, no need to ask. | Low | Directly feeds Issue 7. Frame as story-feel: "Is this a locked-room showdown or a Game-of-Thrones faction war?" |
| 11 | P2 | Builder tournament/retry loop invisible and uncapped | Medium | UX/Arch | Partially addressed — tournament exists but invisible | Surface build-judge loop: "Building... attempt 1/3" with brief failure reason. Cap retries at 3 with "best effort" fallback. After max retries, show judge flags and let user choose: accept, retry with adjusted prompt, or return to clarifier. | Showing failures may reduce confidence. Best-effort fallback may be below threshold. User prompt adjustment is powerful but complex. | Frame failures positively: "Refining your story... (quality check 2/3)" rather than "FAILED, retrying." Don't show the failure reason during generation — only surface it if all 3 attempts fail. For the best-effort fallback, pick the highest-scoring attempt across all 3 (compare total judge scores), not the last one. User prompt adjustment should be a guided experience, not raw text editing — show the specific judge criticism and offer 2-3 targeted adjustments: "The judge flagged [X]. Would you like to: A) Emphasize Y instead, B) Loosen the constraint on Z, C) Accept and move on." Cap at 3 retries with no infinite loops. Log all attempts for debugging. | Medium | TournamentProgress type exists. Users currently see a loading spinner. Even "Building... quality checking..." would help. |

---

## Phase 3: Performance & Readability

| # | Priority | Issue | Severity | Category | Current Status | Recommended Fix | Potential Problems from Fix | Mitigations for Fix Problems | Effort | Notes / Additional Context |
|---|----------|-------|----------|----------|----------------|-----------------|---------------------------|------------------------------|--------|--------------------------|
| 12 | P3 | Scene module takes too long / user input often unnecessary | High | Perf/UX | Partially addressed — auto-pass exists but undertriggered | Four-pronged: (1) "Generate All" mode — all scenes build without interaction. (2) Lower auto-pass threshold from 0.85 to 0.70 for non-turning-point scenes. (3) Batch-build 3-4 scenes in parallel. (4) Skip minor judge for non-critical scenes. | Parallel building increases API burst cost. "Generate All" means no mid-stream steering. Aggressive auto-pass may skip scenes needing input. Skipping judges = undetected quality issues. | For "Generate All" risk: add a post-generation "Director's Review" screen where user reads through all scenes and can flag any for revision. This is faster than per-scene interaction because reading is faster than choosing. For parallel cost: cap at 3 concurrent API calls — still 3x faster than sequential. For auto-pass misses: keep the 0.85 threshold for turning-point and climax scenes (the important ones); only lower to 0.70 for bridging/transition scenes. For skipped judges: run a single "ensemble judge" after all scenes are built that checks coherence across the full story — catches issues individual judges would miss anyway. Add a progress bar: "Scene 4/15 — Turning Point: The Betrayal" so users see meaningful progress, not a frozen spinner. | High | Scene module is biggest time sink: N scenes × (clarifier + builder + judge). "Generate All" is the biggest win. Add "review and revise" step after bulk generation. |
| 13 | P3 | Scene output hard to read — not script-like | Medium | Frontend | Likely frontend issue — data model already has screenplay format | Frontend fix: (1) Render screenplay_text with proper formatting — character names bold/capped, stage directions in gray italics, dialogue indented. (2) Add "Script View" toggle. | Multiple view modes means multiple rendering paths. Different users want different formats. | Start with just ONE well-formatted view: screenplay format (the most universally readable). Add other views later only if users request them. Use a simple markdown-to-styled-HTML renderer for screenplay_text — character names (ALL CAPS lines) get bold, parentheticals get gray italic, stage directions get a left-border accent, dialogue gets standard formatting. This is CSS-only, no complex rendering logic. Test with 3 sample scenes before shipping. If screenplay_text isn't being generated properly by the builder, that's a prompt fix (add explicit formatting instructions to scene builder prompt). | Medium | Check if frontend displays readable_scene.screenplay_text or raw JSON. Fix may be as simple as rendering existing field with proper CSS. Consider also "VN Preview" mode. |
| 14 | P3 | Some modules can't show preview when loading exports | Medium | Frontend | Likely — needs investigation | Audit frontend pack preview components. Each pack type needs a dedicated renderer. Add missing renderers. | 6 custom preview renderers is non-trivial. Pack structures may evolve. Large packs need summarization. | Build ONE generic preview component that renders any pack's `state_summary` field as the primary view (all packs have this field). Then add module-specific detail sections incrementally: hook preview shows hook_sentence + premise, character preview shows character cards, plot preview shows beat timeline. Use the `state_summary` as an immediate fix for all modules — even an ugly text preview is better than a broken blank screen. For large packs, show summary + "Expand details" accordion. Track which modules users actually load previews for — focus rendering effort there. | Medium | Frontend-only fix. Data exists — not displayed. Priority: hook, character, plot, world, scene, characterImage. state_summary field is designed for this. |
| 15 | P3 | Prior user statements not carried forward / implications ignored | High | Prompt/LLM | Architecture correct — LLM inconsistent | Three reinforcements: (1) Move constraint ledger to END of user prompt. (2) Add compact "MUST HONOR" section listing only confirmed constraints. (3) Repeat confirmed constraints as hard requirements in builder. (4) Optional constraint checker LLM call. | Moving ledger position may displace context. Checker adds LLM call. Over-constraining produces rigid output. | Place the "MUST HONOR" block at the very end of the user prompt (last 200 tokens) — this is the highest-attention zone for LLMs. Keep it compact: only confirmed constraints, one line each, all-caps keys. Example: `"PROTAGONIST GENDER: male | SETTING: feudal Japan | TONE: dark romance | STAKES: identity exposure"`. Don't move the full ledger — keep it in its current position for context, and add the compact block as a separate reinforcement. For rigidity risk: the MUST HONOR block only covers confirmed hard facts (gender, setting, tone) — not stylistic choices or inferred constraints. For the constraint checker: make it a FAST-tier call (Haiku) that runs only on the builder output, not the clarifier. It returns pass/fail + specific violations. Only add this if the MUST HONOR block alone doesn't solve the problem — it's a Plan B. | Medium | Fundamentally an LLM attention problem. Highest-impact fix: compact "MUST HONOR" block at prompt end. Also consider "user_direct_statement" source type with higher priority. |
| 16 | P3 | Characters too similar to each other | Medium | Prompt/LLM | Partially addressed — diversity scoring in judge | Four improvements: (1) Differentiation matrix requiring each character to differ on 3+ dimensions. (2) Make diversity a hard-fail judge criterion. (3) Explicit contrast pairs in builder output. (4) Character comparison in pre-builder review. | Forcing differentiation can feel artificial. LLM may default to simple inversions. Hard-fail may cause rebuild loops. | Define differentiation on meaningful dimensions, not surface traits: stress_response (freeze/fight/flee/fawn), communication_style (direct/indirect/manipulative/avoidant), core_value (freedom/control/connection/truth), power_strategy (charm/force/intelligence/endurance). The builder must assign each character different values across at least 3 of these. This prevents simple inversions because the dimensions are multi-valued, not binary. For rebuild loops: make diversity a hard-fail only when 2+ characters share the same value on 3+ dimensions (true duplicates). Softer diversity issues get flagged as warnings, not failures. Add the differentiation matrix as a required output field in the builder response — this forces the LLM to think about it explicitly rather than hoping it emerges naturally. | Medium | Builder has structural_diversity field. Judge scores diversity. Neither aggressive enough. Differentiation matrix is the key addition. |
| 17 | P3 | Unclear if cross-module problem fixing is working | Medium | Transparency | Working — but invisible | Surface upstream dev targets in debug panel (Issue 4). Show "Improved: [area]" indicators. In pre-scene audit (Issue 6), show full target history. | Revealing the system may undermine confidence in earlier modules. Too much machinery breaks the "fun" illusion. | Frame positively: "Your story is getting stronger" with a progress-bar metaphor. Show resolved targets as green checkmarks ("Character depth: strengthened in World module"), not as "the Character module had a weakness." Never use the word "weakness" or "problem" in user-facing text — use "development opportunity" or "area we deepened." In the debug panel (developer mode), show the raw target data for power users. In the main flow, only surface targets if the user explicitly asks or during the pre-scene audit. | Low (if 4+6 built) | Essentially free once debug panel and pre-scene audit exist. Data already in pack handoffs. |
| 18 | P3 | Psychology ledger may accumulate stale signals over 6 modules | Medium | Architecture | Partially addressed — consolidation exists | Add forced "psychology reset" at each module boundary: run consolidation, drop signals with confidence < 0.2, merge duplicates, cap active signals at 8-10. | Aggressive pruning may discard slow-building patterns. Resets lose nuance. | Use a two-tier signal system: "active" signals (confidence >= 0.3, used in current prompt injection) and "archived" signals (confidence < 0.3, kept in storage but not injected into prompts). Archived signals can be promoted back to active if new evidence emerges — the consolidation function already supports this via `reinforcesSignalId`. Cap active signals at 8 (not 10) to keep prompt injection lean. Run forced consolidation at module lock time (after judge, before pack export) — this is a natural boundary. Don't merge signals with different `scope` values (this_story vs global) — they serve different purposes. | Low | Consolidation system exists but fires on throttle timing, not boundaries. Plot module's "MODULE 5 PSYCHOLOGY SHIFT" already says to stop re-confirming old signals. |
| 19 | P3 | Token cost and latency not tracked or displayed | Low | Observability | Not addressed | Add per-session/module token tracker. Display total tokens, estimated cost, time per call, rebuild loop count in debug panel. | Cost display may alarm users. Time tracking adds overhead. | Only show in the debug/developer panel (Issue 4) — never in the main flow. Display as aggregate stats, not per-call breakdowns: "This session: ~45K tokens, ~$0.12, 23 LLM calls." Show the breakdown by module only on expansion. For overhead: the LLMClient response already contains token counts in the API response — just accumulate them in memory, no extra calls needed. Write to session JSON at save time. For alarming users: frame as informational, not a cost warning. Compare to real-world equivalents: "About the cost of a cup of coffee for the whole story." | Low | Useful for optimization. LLMClient likely has response metadata. Just needs aggregation. |

---

## Phase 4: Long-Term / High-Effort

| # | Priority | Issue | Severity | Category | Current Status | Recommended Fix | Potential Problems from Fix | Mitigations for Fix Problems | Effort | Notes / Additional Context |
|---|----------|-------|----------|----------|----------------|-----------------|---------------------------|------------------------------|--------|--------------------------|
| 20 | P3 | Assumptions not deterministic — different runs give different assumptions | Low | Design | By design — dynamic generation | If reproducibility desired: (1) Core assumption template per archetype for turn 1. (2) LLM adds 2-3 dynamic assumptions on top. (3) Lower temperature to ~0.3 for assumption generation. | Templates make experience repetitive. Lower temp reduces creative surprise. Templates may not fit all seed types. | Don't use templates. Instead, ensure category consistency: the prompt should specify "Always surface assumptions for these categories on turn 1: protagonist_gender/presentation, tone, setting, relationship_type, scope." The SPECIFIC content within each category remains LLM-generated (creative), but the CATEGORIES are fixed (predictable). This gives reproducibility where it matters (users always get to weigh in on the important dimensions) without killing surprise (the actual assumption text varies). Don't lower temperature — the current dynamic approach is one of the app's strengths. If a user wants to reproduce a specific session, that's what the seed + constraint ledger export is for. | Low | Current dynamic approach is actually good — each session feels fresh. Middle ground: same categories surfaced, specific content varies. |
| 21 | P4 | Drift concerns across modules | Medium | Architecture | Partially addressed — ledger + psych carry forward | Add "Story Bible" built incrementally at each lock. Contains all confirmed constraints, locked decisions, character profiles, world rules, plot beats. Each module receives full bible. Add consistency check at boundaries. | Bible grows large — context limits. Requires summarization step. Consistency check adds LLM calls. Some "drift" is creative evolution. | Generate the bible as a 300-500 word prose summary using a FAST-tier (Haiku) summarization call at each module lock. Input: previous bible + new pack's key fields. Output: updated prose summary. This grows slowly (adds ~50-100 words per module) and stays within budget. Place the bible in the user prompt's prefix section (cacheable, doesn't change during clarifier turns). For the consistency check: don't add a separate LLM call. Instead, add a "STORY BIBLE — do not contradict" section to the existing builder prompt, and add a "consistency with story bible" dimension to the existing judge. This reuses existing calls with zero added latency. For creative evolution: the bible only contains CONFIRMED facts, not stylistic guidance — the LLM is free to evolve tone and approach within the hard constraints. | High | Packs serve as partial bible but are structured data. LLMs work better with prose summary. A 500-word bible regenerated at each boundary would be more effective. Highest-effort fix but most comprehensive. |
| 22 | P4 | No way to go back and revise a locked module | Medium | UX Gap | Not addressed — strictly linear | Add "Unlock and Revise" letting user return to locked module. Revised module re-runs builder/judge. Downstream modules marked for regeneration. Show warning about cascading changes. | Extremely complex. Downstream decisions based on original output. Cascade produces different results. Users lose downstream work. State management becomes complex. | **Phase this feature in three stages.** Stage 1 (low effort): Allow re-running the builder/judge of the CURRENT module (before proceeding) with modified constraints — this is just a "regenerate" button with optional prompt edits. Stage 2 (medium effort): Allow unlocking the PREVIOUS module only (one step back). Store the current downstream state as a "snapshot" the user can restore if they don't like the revision. Stage 3 (high effort): Full multi-step rollback with cascade regeneration. For all stages: never auto-delete downstream work — mark it as "stale" and let the user choose to regenerate or keep. Show a clear diff: "Here's what changed" after re-generation. For state management: use a branching model (like git) — each revision creates a new branch, original branch is preserved. | Very High | Major feature many creative tools support. Lighter version: allow re-running builder/judge of current module before proceeding. |
| 24 | P2 | App never advises user about scope/length/cast impact before they commit | High | UX / Prompt | Not addressed — scope/length and cast size are passive constraints with no advisory | Add a **Story Scope Advisor** that activates around hook turns 2-3. When the story shape is forming, the clarifier should proactively analyze the seed and present a scope recommendation explaining: (1) how many characters this story naturally needs and why, (2) what length/pacing feels right and why, (3) what this means for the user's experience (generation time, scene count, depth vs breadth trade-off). Frame as "Here's what I'm seeing for this story" with clear alternatives: "This story works best as a tight 2-character slow burn (~8 scenes, ~15 min generation). But if you want more faction intrigue, we could expand to 5 characters and ~15 scenes (~30 min generation)." Also surface post-analysis recommendations per module: character module says "4 characters is right for this triangle + mentor dynamic" or "you could add a 5th but they'd get less depth — worth it?" | Users may feel lectured or slowed down. Advisory may anchor choices (recommendation bias). Adds text to already-long clarifier responses. Could feel like the app is telling users what they want. | Frame as enthusiasm, not instruction: "I think this story WANTS to be a tight three-person pressure cooker — here's why..." not "I recommend 3 characters." Keep advisory to 2-3 sentences max — not a wall of text. Make it part of the natural clarifier conversation, not a separate modal/screen. Provide the recommendation WITH the assumption so the user can accept, modify, or ignore in the same interaction. Never block progress on scope acceptance — if the user ignores it, infer from context and move on. For recommendation bias: always present at least 2 genuine alternatives with honest trade-offs, not just "my recommendation" + obviously worse options. In the character module, surface a brief reminder: "We settled on 4 characters in the hook — still feeling right, or should we adjust?" | Medium | Directly strengthens Issues 5, 7, and 10. The scope/length creative constraint already exists in the hook clarifier (line 131-132) — it just never explains what the choice MEANS. The advisory should live inside the existing assumption/constraint flow, not as a separate system. Key insight: the app knows things the user doesn't (generation time, scene count implications, depth trade-offs) and should share that knowledge proactively. |

---

## Summary

| Metric | Count | Details |
|--------|-------|---------|
| **Total Issues** | 24 | 15 user-reported + 3 code review + 6 additional discoveries |
| Critical Severity | 3 | Feature flag, end-to-end testing, gender/sex field |
| High Severity | 8 | Scene cultural, debug panel, length scaling, loose ends, character editing, scene speed, carry-forward, scope advisor |
| Medium Severity | 10 | Cast size, warnings, similarity, readability, previews, drift, psych signals, tournament, cast count, cross-module |
| Low Severity | 3 | Assumption determinism, token tracking, psych stale signals |

| Effort Level | Count |
|-------------|-------|
| Trivial | 1 |
| Low | 5 |
| Medium | 9 |
| Medium-High | 2 |
| High | 4 |
| Very High | 1 |

---

### Key Dependencies
- Issue 17 (cross-module visibility) is free once Issues 4 (debug panel) and 6 (pre-scene audit) are built
- Issue 7 (cast size) is strengthened by Issue 10 (cast count in hook)
- Issue 8 (character editing) could leverage the cultural engine for name/description suggestions
- Issue 8 (character editing) also partially solves Issue 23 (gender) by giving users a direct override
- Issue 12 (scene speed) partially mitigates Issue 3 (scene cultural integration) concerns about added latency
- Issue 21 (drift / story bible) is the comprehensive fix that partially addresses Issues 15, 16, and 17
- Issue 23 (gender) extends to age/race/ethnicity — same fix pattern for all physical identity fields
- Issue 15 (MUST HONOR block) reinforces Issues 4, 23, and 21 — confirmed constraints stay honored across modules
- Issue 24 (scope advisor) directly strengthens Issues 5 (beat count), 7 (cast size), and 10 (cast in hook) — the advisor gives users context BEFORE those constraints lock in
- Issue 24 (scope advisor) + Issue 9 (warnings) form a complete guidance system: Issue 24 handles "you should know this before choosing" and Issue 9 handles "your choice is causing a problem"
